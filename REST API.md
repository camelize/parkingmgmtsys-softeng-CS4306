# FastAPI

As it is now, it only runs on local devices and host. Needing some more fixes area, but the server is responding to the client.


# Sync with Database

As of now, it is not pairing with the database nor syncing. We still need the database to store the data permanently after receiving from the OpenCV + OCR.
.
# Request

Missing request body, but execution still works proper. Manually adding in the ID, plate number, etc. for now. Can handle empty requests.

**This was pulled in from multiple sources i.e. Github, GeeksforGeeks, LLMs**
